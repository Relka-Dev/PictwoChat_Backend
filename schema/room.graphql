type Room {
    _id: String!
    name: String!
    createdAt: String!
    createdBy: String
}

extend type Query {
    rooms: [Room]
}

extend type Mutation {
    createRoom(name: String!): Room
    removeRoom(_id: String!): String
}